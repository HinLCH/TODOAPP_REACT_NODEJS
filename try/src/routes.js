import Todoapp from './Todoapp'
import Login from './Login'

const routes = [
    {
        path: '/todoapp',
        component: Todoapp
    },
    {
        path: '/login',
        component: Login
    }
];

export default routes;